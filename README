RubyMinds is the personal website of Mike Judge, but more interestingly than that, it's an example of a blog
powered by Redis instead of a regular database.  As an experiment, it turned out rather well:

* Without doing any Rails caching, pages render typically in 0.01 - 0.03 seconds

